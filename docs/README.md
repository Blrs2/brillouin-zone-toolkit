# Documentation artifacts

`figures/` and `summary.json` are generated by `python scripts/reproduce.py`.

The source PDF is intentionally kept outside the public repository until redistribution rights
are confirmed. The original remains available as `../BrilloinZones_IC.pdf` in the supplied local
archive. The repository's MIT license does not relicense that document.
